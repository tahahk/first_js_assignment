/*6. Write a program to take input max age & current age from
user. If the current age is less than or equal to max age, show the message 
“You are welcome”*/

var maxage = prompt("Enter Your Max age",maxage);
var yourage = prompt("Enter your age",yourage);

if(yourage<=maxage){
    document.write("You are Welcome");
}